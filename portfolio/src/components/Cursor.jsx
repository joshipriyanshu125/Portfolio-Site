import { useEffect, useState } from 'react';
export default function Cursor(){ const [p,setP]=useState({x:-80,y:-80}); const [active,setActive]=useState(false); useEffect(()=>{const move=e=>setP({x:e.clientX,y:e.clientY}); const over=e=>setActive(!!e.target.closest('a,button,input,textarea')); window.addEventListener('pointermove',move); window.addEventListener('pointerover',over); return()=>{window.removeEventListener('pointermove',move);window.removeEventListener('pointerover',over)}} ,[]); return <i className={'cursor '+(active?'cursor-active':'')} style={{transform:`translate(${p.x}px,${p.y}px)`}} /> }
